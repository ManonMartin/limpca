---
title: "Analyse des 4 dataset Candies,Human Serum, CHOO et UCH avec les fonctions limpca updates"
author: "Visschers Antoine"
date: "21/11/2023"
output:
  html_document: # options pour sortie HTML
    code_folding: hide #  Cache le code (mais permet de l'ouvrir :-))
    collapsed: yes # Crée un document unique 
    fig_caption: yes # Figures encapsulées ? 
    fig_height: 5 # Hauteur par défaut des figures
    fig_width: 6 # Largeur par défaut des figures
    highlight: tango # style de mise en valeur du code
    number_sections: yes # Ajout table des matières 
    theme: united  # Style du document
    toc: yes # Table des matiere 
    toc_depth: 4  # Profondeur de la table des matière
    toc_float: yes # table des matière flottante
editor_options: 
  chunk_output_type: console
  markdown: 
    wrap: 72
---

```{r setup, include=FALSE}
rm(list=ls())
knitr::opts_chunk$set(echo = TRUE)
# remotes::install_github("manonmartin/mbxucl")
require(tidyverse)
require(MBXUCL)
require(lme4)
require(plyr)
require(pander)
require(grid)
require(stringr)
require(gridExtra)
require(ggplot2)
require(cowplot)
require(stringr) # add leading zeros to volunteer number
require(reshape2)
# pak::pak("spatstat")
require(spatstat) # compute quantiles based on 
require(emdbook) # mixture chi2
require(ggpubr)
require(tidyr) # for gather function
# require(mdatools)
require("car")
require(dplyr)
# pak::pak("metafolio")
require(metafolio)
# library(limpca)
library(kableExtra)

# # Chemin pour les outputs
# fig_path <- "./test_limpca_outputs/"
# 
# # chemin pour mettre dans le fichier Candies
# output_Candies <- paste0(fig_path,"/Candies")
# 
# # chemin pour mettre dans le fichier Serum
# output_Serum = paste0(fig_path,"/Serum")
# 
# # chemin pour mettre dans le fichier CHOO
# output_CHOO = paste0(fig_path,"/CHOO")
# 
# # chemin pour mettre dans le fichier UCH
# output_UCH = paste0(fig_path,"/UCH")

#charge toute les fonction de limpca
devtools::load_all(".")

#charge toute les fonctions de limpca modifié
# map(list.files("../limpca/inst/doc/R_new_update/", full.names = TRUE), source)
```

```{bash}
quarto preview test_limpca_modemixtes.Rmd \
  --no-browser \
  --no-watch-inputs \
  --embed-resources
```

# Importation des données

Importation des quatres RData qui contiennent :

-   design : le plan d'expérience
-   outcomes : la matrice réponses
-   formula : la formule du modèle
-   model : l'acronyme du type de modèle ("lmm" = linear mixed model ou
    "lm" = linear model). Cet élément est optionnel et n'est pas dans les RData importés.

```{r, results='hide', echo=FALSE}
# 1.Data Candies
data("Candies")

# 2.Data Human Serum
data("Serum")

# 3.CHOO
data("CHOO")

#4.UCH
data("UCH")
str(UCH)
```

## lmpDataList :

l'objet lmpDataList de l'ensemble Candies est :

```{r}
str(Candies)
```

l'objet lmpDataList de l'ensemble Serum est :

```{r}
str(Serum)

# FIXME
rownames(Serum$design) <- rownames(Serum$outcomes)
```

l'objet lmpDataList de l'ensemble CHOO est :

```{r}
 str(CHOO)
```

l'objet lmpDataList de l'ensemble UCH est :

```{r}
 str(UCH)
```

# Exploration des données

## Partie Candies

### Visualisation du plan d'expérience

```{r}
plotDesign(design = Candies$design, x = "Judges", 
           y = "Candies",
           title = "Plan de l'ensemble Candies")

# #exporter le plan
# ggsave(plot = last_plot(), device = NULL, filename = file.path(output_Candies,"design_Candies.jpeg"),
#   scale = 0.6, width = 15, height = 7, units = "cm",dpi = 1000)
```

Les données sont bien balancées.

### Visualisation de la matrice des réponses:

Structure de la matrice des réponses : 

```{r}
str(Candies$outcomes)
```

```{r}
p1 <- plotLine(Y = Candies$outcomes,
         title = "plotLine de trois observations sur Candies",
         rows = c(4,50,12),
         xlab = "Critères",
         ylab = "Notes", 
         xaxis_type = "character", 
         type = "s")

# ggsave(file = file.path(output_Candies,"plotLine_Candies.jpeg"),p1)
p1
```

```{r}
plotScatterM(Y = Candies$outcomes, cols = c(1:3), 
             design = Candies$design,varname.colorup = "Candies",varname.colordown = "Candies", varname.pchup = "Judges", varname.pchdown = "Judges",  title = "plotScatterM de trois des réponses")
```

```{r}
plotScatter(Y = Candies$outcomes,
            xy = c("Hard","Acid"),
            design = Candies$design,
            color = "Judges", 
            shape = "Candies",
            title = "plotScatter entre les notes Hard et les notes Acid")
```

```{r}
plotMeans(Y = Candies$outcomes,
          design = Candies$design,
          cols = c("Hard"),
           x = c("Candies"),
          z = c("Judges"),
          ylab = "Notes",
          title=c("Moyennes des notes du critère Hard"))
```

## Partie Serum

### Visualisation du plan d’expérience

```{r}
plotDesign(design = Serum$design, x = "Volunteer", 
           y = "Sampling",
           title = "Plan de l'ensemble Serum")

```

Les données ne sont pas balancées, il manque un résultat pour le
deuxième échantillon du quatrième et huitième volontaire et 2 résultats
pour le premier échantillon du onzième volontaire.

### Visualisation de la matrice des réponses:

Structure de la matrice des réponses : 

```{r}
str(Serum$outcomes)
```
```{r}
p2 <- plotLine(Y = Serum$outcomes,
         title = "plotLine de trois observations sur Serum",
         rows = c(3,17,12),
         xlab = "ppm",
         xaxis_type = "numeric", 
         type = "s")
p2
```

```{r}
plotScatterM(Y = Serum$outcomes, cols = c(1:3), 
             design = Serum$design,varname.colorup = "Volunteer",varname.colordown = "Volunteer", varname.pchup = "Sampling", varname.pchdown = "Sampling",  title = "plotScatterM de trois des réponses")

```

```{r}
plotScatter(Y = Serum$outcomes,
            xy = c(3,142),
            design = Serum$design,
            color = "Volunteer", 
            shape = "Sampling",
            title = "plotScatter entre 2 réponses de Serum")
```

```{r}
plotMeans(Y = Serum$outcomes,
          design = Serum$design,
          cols = c(15),
           x = c("Sampling"),
          z = c("Volunteer"),
          ylab = "Intensité",
          title=c("Moyennes des intensités de la réponse 15"))
```


## Partie CHOO

### Visualisation du plan d’expérience

```{r}
plotDesign(design = CHOO$design[CHOO$design[,"traitement"]=="Control",], x = "volunteer", 
           y = "time", cols = "traitement",
           title = "Plan du traitement Control de CHOO")

plotDesign(design = CHOO$design[CHOO$design[,"traitement"]=="Antibiotic",], x = "volunteer", 
           y = "time", cols = "traitement",
           title = "Plan du traitement Antibiotic de CHOO")

```

On peut voir que les données ne sont pas balancées. Il manque le 3ème
résultat du sujet 6 dans le groupe Antibiotic et le 2ème résultat du
sujet 1 dans le groupe Antibiotic.

### Visualisation de la matrice des réponses:

Structure de la matrice des réponses : 

```{r}
str(CHOO$outcomes)
```
```{r}
p3 <- plotLine(Y = CHOO$outcomes,
         title = "plotLine de trois observations sur CHOO",
         rows = c(3,17,12),
         xlab = "ppm",
         xaxis_type = "character", 
         type = "s")
p3
```

```{r}
plotScatterM(Y = CHOO$outcomes, cols = c(1:3), 
             design = CHOO$design,varname.colorup = "time",varname.colordown = "time", varname.pchup = "treatment", varname.pchdown = "treatment",  title = "plotScatterM de trois des réponses")

```

```{r}
plotScatter(Y = CHOO$outcomes,
            xy = c(3,142),
            design = CHOO$design,
            color = "time", 
            shape = "treatment",
            title = "plotScatter entre 2 réponses de CHOO")
```

```{r}
plotMeans(Y = CHOO$outcomes,
          design = CHOO$design,
          cols = c(15),
           x = c("time"),
          z = c("treatment"),
          ylab = "Intensité",
          title=c("Moyennes des intensités de la réponse 15"))
```


## Partie UCH

Utilisation limitée aux variables Citrate, Hippurate et Time

### Visualisation du plan d’expérience

```{r}
plotDesign(design = UCH$design, x = "Hippurate", 
           y = "Citrate", rows = "Time",
           title = "Design of the UCH dataset")
```

Le modèle n'est pas balancé, il manque une observation lorsque le
Citrate = 2, l'Hippurate = 0 et le temps = 1 et une observation lorsque le Citrate = 4, l'Hippurate = 0 et le temps = 1.

### Visualisation de la matrice des réponses:

Structure de la matrice des réponses : 

```{r}
str(UCH$outcomes)
```
```{r}
p4 <- plotLine(Y = UCH$outcomes,
         title = "H-NMR spectrum",
         rows = c(3),
         xlab = "ppm",
         ylab = "Intensité")

p4
```

```{r}
plotScatterM(Y = UCH$outcomes, cols = c(133, 145, 150, 369, 453), 
             design = UCH$design,varname.colorup = "Hippurate", 
             varname.colordown = "Citrate")

```

```{r}
plotScatter(Y = UCH$outcomes,
            xy = c("2.6092056","3.9811536"),
            design = UCH$design,
            color = "Hippurate", 
            shape = "Citrate")
```

```{r}
plotMeans(Y = UCH$outcomes,
          design = UCH$design,
          cols = c(453),
           x = c("Citrate"),
          w = c("Hippurate"),
          z = c("Time"),
          ylab = "Intensity",
          title=c("Moyennes de l'intensité de la réponse 453"))
```

# PCA sur les outcomes

Remplacer les outcomes par les scores des PCA pour diminuer le nombre de
variables et donc de paramètres à estimer. Cette étape n'a pas été faite pour le modèle linéaire UCH.

## Partie Candies

```{r}
# source("inst/doc/R_new_update/NEW_lmpOutcomesReduct.R")

resPCA_Candies <- lmpOutcomesReduct(Candies)
lmpDataList_Candies <- resPCA_Candies$lmpDataList

nPC_Candies <- resPCA_Candies$nPC
resPCA_Candies <- resPCA_Candies$resPCA

pcaScreePlot(resPCA_Candies, nPC = nPC_Candies,  title = "PCA sur les outcomes")


pcaScorePlot(resPcaBySvd = resPCA_Candies, axes = c(1,2), 
title = "Score plot Judges", 
design = Candies$design, color="Judges", drawShapes = "segment",
points_labs_rn = FALSE)

pcaScorePlot(resPcaBySvd = resPCA_Candies, axes = c(1,2), 
title = "Score plot Candies", 
design = Candies$design, color="Candies", drawShapes = "polygon",
points_labs_rn = FALSE)

pcaLoading2dPlot(resPcaBySvd = resPCA_Candies, axes = c(1,2), title = "PCA loadings plot candies",pl_n = 9, addRownames = TRUE)

#research columns
spectra_PCA_scores <- resPCA_Candies$scores
spectra_PCA_loadings_Candies <- resPCA_Candies$loadings


# outcomes = PCA score
outcomes_Candies <- spectra_PCA_scores
rownames(outcomes_Candies) <- rownames(Candies$design)
```


## Partie Serum

```{r}
n <- nrow(Serum$outcomes)

resPCA_Serum <- lmpOutcomesReduct(Serum)
lmpDataList_Serum <- resPCA_Serum$lmpDataList
nPC_Serum <- resPCA_Serum$nPC
resPCA_Serum <- resPCA_Serum$resPCA

pcaScreePlot(resPCA_Serum, nPC_Serum, title = "PCA sur les outcomes")


pcaScorePlot(resPcaBySvd = resPCA_Serum, axes = c(1,2), 
title = "Score plot Sampling", 
design = Serum$design, color="Sampling", drawShapes = "segment",
points_labs_rn = FALSE)


pcaScorePlot(resPcaBySvd = resPCA_Serum, axes = c(1,2), 
title = "Score plot Volunteer", 
design = Serum$design, color="Volunteer", drawShapes = "polygon",
points_labs_rn = FALSE)



pcaLoading2dPlot(resPcaBySvd = resPCA_Serum, axes = c(1,2), title = "PCA loadings plot Serum", addRownames = TRUE)

#research columns
spectra_PCA_scores <- resPCA_Serum$scores
spectra_PCA_loadings_Serum <- resPCA_Serum$loadings


# outcomes = PCA score
outcomes_Serum <- spectra_PCA_scores
rownames(outcomes_Serum) <- rownames(Serum$design)
```

## Partie CHOO

```{r}
n <- nrow(CHOO$outcomes)

resPCA_CHOO <- lmpOutcomesReduct(CHOO)
lmpDataList_CHOO <- resPCA_CHOO$lmpDataList
nPC_CHOO <- resPCA_CHOO$nPC
resPCA_CHOO <- resPCA_CHOO$resPCA

pcaScreePlot(resPCA_CHOO, nPC = nPC_CHOO, title = "PCA sur les outcomes")


pcaScorePlot(resPcaBySvd = resPCA_CHOO, axes = c(1,2), 
title = "Score plot Traitement", 
design = CHOO$design, color="traitement", drawShapes = "segment",
points_labs_rn = FALSE)

pcaScorePlot(resPcaBySvd = resPCA_CHOO, axes = c(1,2), 
title = "Score plot Time", 
design = CHOO$design, color="time", drawShapes = "segment",
points_labs_rn = FALSE)


# pcaScorePlot(resPcaBySvd = resPCA_CHOO, axes = c(1,2), 
# title = "Score plot Time:Treatment", 
# design = CHOO$design, color="timetreatment", drawShapes = "segment",
# points_labs_rn = FALSE)


pcaScorePlot(resPcaBySvd = resPCA_CHOO, axes = c(1,2), 
title = "Score plot Volunteer", 
design = CHOO$design, color="volunteer", drawShapes = "polygon",
points_labs_rn = FALSE)

pcaLoading2dPlot(resPcaBySvd = resPCA_CHOO, axes = c(1,2), title = "PCA loadings plot Serum", addRownames = TRUE)

#research columns
spectra_PCA_scores <- resPCA_CHOO$scores
spectra_PCA_loadings_CHOO <- resPCA_CHOO


# outcomes = PCA score
outcomes_CHOO <- spectra_PCA_scores
rownames(outcomes_CHOO) <- rownames(CHOO$design)
```



## lmpDataList :

### Partie Candies

```{r}
str(lmpDataList_Candies)
```


### Partie Serum

```{r}
str(lmpDataList_Serum)
```

### Partie CHOO

```{r}
str(lmpDataList_CHOO)
```


# Estimation du modèle et décomposition de la matrice d'effets

## Estimation des matrices de modèles

### Partie Candies

```{r}
resLmpModelMatrix_Candies <- lmpModelMatrix(lmpDataList_Candies)
```

### Partie Serum

```{r}
resLmpModelMatrix_Serum <- lmpModelMatrix(lmpDataList_Serum)
```

### Partie CHOO

```{r}
resLmpModelMatrix_CHOO <- lmpModelMatrix(lmpDataList_CHOO)
```


### Partie UCH

```{r}
resLmpModelMatrix_UCH <- lmpModelMatrix(UCH)
```

## Estimations des modèles mixtes

### Partie Candies

#### Estimation avec la variance des effets fixes calculée avec la formule du type 3 SS de limpca (resLmpEffectMatrices_Candies).

```{r}
resLmpEffectMatrices_Candies <-  lmpEffectMatrices(resLmpModelMatrix_Candies)

resLmpEffectMatrices_Candies$type3SS
```


#### Résultat du modèles pour certaines réponses

##### Résultat du modèle pour la PC1

```{r}
resLmpEffectMatrices_Candies$MM_full$PC1
```

##### Résultat du modèle pour la PC5

```{r}
resLmpEffectMatrices_Candies$MM_full$PC5
```

##### Résultat du modèle pour la PC6

```{r}
resLmpEffectMatrices_Candies$MM_full$PC6
```

##### Résultat du modèle pour la PC8

```{r}
resLmpEffectMatrices_Candies$MM_full$PC8
```

### Partie Serum

```{r}
resLmpEffectMatrices_Serum <-  lmpEffectMatrices(resLmpModelMatrix_Serum)
```


#### Résultat du modèles pour certaines réponses

##### PC8 avec l'optimisation bobyqa

```{r}
summary(resLmpEffectMatrices_Serum$MM_full$PC8)
# optimisation: lmerControl(optimizer = "bobyqa")
```

##### PC8 avec l'optimisation Nelder-Mead

```{r}
summary(lmer(formula = formula(paste0("PC8",lmpDataList_Serum$formula)),data = cbind(lmpDataList_Serum$design,lmpDataList_Serum$outcomes)))
```

### Partie CHOO


```{r}
### parallel LMM 
#######################
resLmpEffectMatrices_CHOO <-  lmpEffectMatrices(resLmpModelMatrix_CHOO)

```



### Partie UCH

```{r}
resLmpEffectMatrices_UCH <-  lmpEffectMatrices(resLmpModelMatrix_UCH)
resLmpEffectMatrices_UCH$varPercentagesPlot
```

# Pourcentage de variance expliqué par effet :

## Partie Candies

```{r}
pander::pander(resLmpEffectMatrices_Candies$variationPercentages)
resLmpEffectMatrices_Candies$varPercentagesPlot
```

## Partie Serum

```{r}
pander::pander(resLmpEffectMatrices_Serum$variationPercentages)
resLmpEffectMatrices_Serum$varPercentagesPlot
```

## Partie CHOO


```{r}
pander::pander(resLmpEffectMatrices_CHOO$variationPercentages)
resLmpEffectMatrices_CHOO$varPercentagesPlot
```

## Partie UCH

```{r}
pander::pander(resLmpEffectMatrices_UCH$variationPercentages)
resLmpEffectMatrices_UCH$varPercentagesPlot
```

# Tests Bootstrap

## Partie Candies

```{r,eval=FALSE}
resLmpBootstrapTests_Candies <- lmpBootstrapTests(resLmpEffectMatrices_Candies, nboot = 20, verbose = TRUE)
save(resLmpBootstrapTests_Candies, file = "./test_limpca_outputs/resBootstrap_Candies.rda")
```

### Significativité des effets

```{r}
load(file = "./test_limpca_outputs/resBootstrap_Candies.rda")
pander::pander(t(resLmpBootstrapTests_Candies$resultsTable))
```

### Distribution bootstrap par effet

```{r}
for(name in names(resLmpBootstrapTests_Candies$f.obs)){
m=hist(resLmpBootstrapTests_Candies$f.boot[[name]], freq=F, breaks=100,
       xlab="Global Likelihood Ratio Statistic",
       xlim=range(resLmpBootstrapTests_Candies$f.obs[name], resLmpBootstrapTests_Candies$f.boot[[name]]),
       ylim = c(0,0.08),
       col = "gray75",border = "gray75",
       main = paste( name , "effect"), cex.main = 2.2)
points(resLmpBootstrapTests_Candies$f.obs[name], 0, col="red", pch=19, lwd=6)
legend("topright",
       legend = c(paste0("True GLRT: ", round(resLmpBootstrapTests_Candies$f.obs[name],2))),
       col = c("red"),pch=19, inset=c(-0.1,0),box.lty=0, cex = 1.4, y.intersp = 0.8)
m
}
```

Alternative au graphe précédent
```{r}
# Bootstrap values in long format
df_boot <- as.data.frame(resLmpBootstrapTests_Candies$f.boot) |>
  pivot_longer(
    cols = everything(),
    names_to = "effect",
    values_to = "glrt_boot"
  )

# True GLRT values
df_true <- data.frame(
  effect = names(resLmpBootstrapTests_Candies$f.obs),
  glrt_true = as.numeric(resLmpBootstrapTests_Candies$f.obs)
)


df_true <- df_true |>
  mutate(
    facet_label = paste0(
      effect, "\nTrue GLRT = ", round(glrt_true, 2)
    )
  )

df_boot <- df_boot |>
  left_join(df_true, by = "effect")

ggplot(df_boot, aes(x = glrt_boot)) +

  # Histogram
  geom_histogram(
    aes(y = after_stat(density)),
    bins = 100,
    fill = "grey75",
    color = "grey75"
  ) +

  # True GLRT line
  geom_vline(
    data = df_true,
    aes(xintercept = glrt_true, colour = "True GLRT"),
    linewidth = 1
  ) +

  # Facets
  facet_wrap(
    ~ facet_label,
    scales = "free_x"
  ) +

  # Labels
  labs(
    title = "Bootstrap Distribution of Global Likelihood Ratio Statistics",
    x = "Global Likelihood Ratio Statistic",
    y = "Density",
    colour = NULL
  ) +

  # Styling
  scale_colour_manual(values = c("True GLRT" = "red")) +

  theme_minimal(base_size = 13) +
  theme(
    legend.position = "top",
    strip.text = element_text(face = "bold", size = 11),
    plot.title = element_text(face = "bold", size = 16)
  )

```

## Partie Serum

```{r,eval=FALSE}
resLmpBootstrapTests_Serum <- lmpBootstrapTests(resLmpEffectMatrices_Serum, nboot = 20, verbose = TRUE)
save(resLmpBootstrapTests_Serum, file = "./test_limpca_outputs/resBootstrap_Serum.rda")
```

### Significativité des effets

```{r}
load(file = "./test_limpca_outputs/resBootstrap_Serum.rda")
pander::pander(t(resLmpBootstrapTests_Serum$resultsTable))
```

### Distribution bootstrap par effet

```{r}
for(name in names(resLmpBootstrapTests_Serum$f.obs)){
m=hist(resLmpBootstrapTests_Serum$f.boot[[name]], freq=F, breaks=100,
       xlab="Global Likelihood Ratio Statistic",
       xlim=range(resLmpBootstrapTests_Serum$f.obs[name], resLmpBootstrapTests_Serum$f.boot[[name]]),
       ylim = c(0,0.08),
       col = "gray75",border = "gray75",
       main = paste( name , "effect"), cex.main = 2.2)
points(resLmpBootstrapTests_Serum$f.obs[name], 0, col="red", pch=19, lwd=6)
legend("topright",
       legend = c(paste0("True GLRT: ", round(resLmpBootstrapTests_Serum$f.obs[name],2))),
       col = c("red"),pch=19, inset=c(-0.1,0),box.lty=0, cex = 1.4, y.intersp = 0.8)
m
}
```

## Partie CHOO

```{r, eval=FALSE}
resLmpBootstrapTests_CHOO <- lmpBootstrapTests(resLmpEffectMatrices_CHOO, nboot = 20, verbose = TRUE)
save(resLmpBootstrapTests_CHOO, file = "./test_limpca_outputs/resBootstrap_CHOO.rda")
```

### Significativité des effets

```{r}
load(file = "./test_limpca_outputs/resBootstrap_CHOO.rda")
pander::pander(t(resLmpBootstrapTests_CHOO$resultsTable))
```

### Distribution bootstrap par effet

```{r}
for(name in names(resLmpBootstrapTests_CHOO$f.obs)){
m=hist(resLmpBootstrapTests_CHOO$f.boot[[name]], freq=F, breaks=100,
       xlab="Global Likelihood Ratio Statistic",
       xlim=range(resLmpBootstrapTests_CHOO$f.obs[name], resLmpBootstrapTests_CHOO$f.boot[[name]]),
       ylim = c(0,0.08),
       col = "gray75",border = "gray75",
       main = paste( name , "effect"), cex.main = 2.2)
points(resLmpBootstrapTests_CHOO$f.obs[name], 0, col="red", pch=19, lwd=6)
legend("topright",
       legend = c(paste0("True GLRT: ", round(resLmpBootstrapTests_CHOO$f.obs[name],2))),
       col = c("red"),pch=19, inset=c(-0.1,0),box.lty=0, cex = 1.4, y.intersp = 0.8)
m
}
```

## Partie UCH

```{r,eval=FALSE}
resLmpBootstrapTests_UCH <- lmpBootstrapTests(resLmpEffectMatrices_UCH, nboot = 20, verbose = TRUE)
save(resLmpBootstrapTests_UCH, file = "./test_limpca_outputs/resBootstrap_UCH.rda")
```

### Significativité des effets

```{r}
load(file = "./test_limpca_outputs/resBootstrap_UCH.rda")
pander::pander(t(resLmpBootstrapTests_UCH$resultsTable))
```

### Distribution bootstrap par effet

```{r}
for(name in names(resLmpBootstrapTests_UCH$f.obs)){
m=hist(resLmpBootstrapTests_UCH$f.boot[,name], freq=F, breaks=100,
       xlab="Global Likelihood Ratio Statistic",
       xlim=range(resLmpBootstrapTests_UCH$f.obs[name], resLmpBootstrapTests_UCH$f.boot[,name]),
       ylim = c(0,0.08),
       col = "gray75",border = "gray75",
       main = paste( name , "effect"), cex.main = 2.2)
points(resLmpBootstrapTests_UCH$f.obs[name], 0, col="red", pch=19, lwd=6)
legend("topright",
       legend = c(paste0("True GLRT: ", round(resLmpBootstrapTests_UCH$f.obs[name],2))),
       col = c("red"),pch=19, inset=c(-0.1,0),box.lty=0, cex = 1.4, y.intersp = 0.8)
m
}
```

# Calcul des dimensions effectives (ED) et facteurs de corrections

```{r}
# calcul du facteur de correction
fact_corretion <- function(df1, df2, min_df2 = 4){
   if (any(df2 < min_df2)) {
     warning("The degree of freedom df2 is too small. Using df2 = ", min_df2,"\n")
     df2[df2 < min_df2] <- min_df2
   }
      
  Fstat <- qf(.95, df1=df1 ,df2= df2)
  coef <- sqrt((Fstat*df1)/df2)
  return(coef)
}
```

## Partie Candies

### ED

```{r}
# source("inst/doc/R_new_update/computeED.R")
res_ED_Candies <- computeED(resLmpEffectMatrices_Candies)
pander::pander(round(res_ED_Candies,1))
```

### Facteurs de corrections

Mixed ANOVA 2 with interaction -> add random interaction to test for Candies

```{r}
fact_corretion_Candies <- fact_corretion(res_ED_Candies["Candies",], res_ED_Candies["Candies:Judges",])
fact_corretion_Judges <- fact_corretion(res_ED_Candies["Judges",], res_ED_Candies["Residuals",])
fact_corretion_CJ <- fact_corretion(res_ED_Candies["Candies:Judges",], res_ED_Candies["Residuals",])

facts_corretions_Candies <- rbind(Candies = fact_corretion_Candies, Judges = fact_corretion_Judges, `Candies:Judges` = fact_corretion_CJ)

pander::pander(round(facts_corretions_Candies, 1))
```

## Partie Serum

### ED

```{r}
res_ED_Serum <- computeED(resLmpEffectMatrices_Serum)
pander::pander(round(res_ED_Serum,1))
```

### Facteurs de corrections

Nested mixed ANOVA 2 -> add random interaction to test for Volunteer

```{r}
fact_corretion_VS <- fact_corretion(res_ED_Serum["Volunteer:Sampling",], res_ED_Serum["Residuals",])
fact_corretion_Volunteer <- fact_corretion(res_ED_Serum["Volunteer",], res_ED_Serum["Volunteer:Sampling",])

facts_corretions_Serum <- rbind(`Volunteer:Sampling` = fact_corretion_VS, Volunteer = fact_corretion_Volunteer)

pander::pander(round(facts_corretions_Serum, 1))

```

## Partie CHOO

### ED

```{r}
res_ED_CHOO <- computeED(resLmpEffectMatrices_CHOO)
pander::pander(round(res_ED_CHOO,1))
```

### Facteurs de corrections

Longitudinal 2 factors -> add random effect (volunteer) to test for treatment

```{r}
fact_corretion_volunteer <- fact_corretion(res_ED_CHOO["volunteer",], res_ED_CHOO["Residuals",])
fact_corretion_treatment <- fact_corretion(res_ED_CHOO["treatment",], res_ED_CHOO["volunteer",])
fact_corretion_time <- fact_corretion(res_ED_CHOO["time",], res_ED_CHOO["Residuals",])
fact_corretion_TrTi <- fact_corretion(res_ED_CHOO["treatment:time",], res_ED_CHOO["Residuals",])

facts_corretions_CHOO <- rbind(volunteer = fact_corretion_volunteer,treatment = fact_corretion_treatment, time = fact_corretion_time, `treatment:time` = fact_corretion_TrTi)

pander::pander(round(facts_corretions_CHOO, 1))

```

# ACP sur les matrices d'effets

## ASCA

```{r}
# source("inst/doc/R_new_update/lmpPcaEffects3.R")
# source("inst/doc/R_new_update/computeED.R")
# source("inst/doc/R_new_update/computeAugmentedScoresAPCA.R")
# source("inst/doc/R_new_update/computeAugmentedScoresASCAE.R")
```

### Partie Candies

```{r}
# with ED
# resLmpPcaEffectsASCA_Candies <- lmpPcaEffects(resLmpEffectMatrices_Candies, method="ASCA",verbose = TRUE,backtransform = TRUE, correctedMatrixAdd = FALSE)

resLmpPcaEffectsASCA_Candies <- lmpPcaEffects(resLmpEffectMatrices_Candies, method="ASCA",
                                        verbose = TRUE,backtransform = TRUE, 
                                        correctedMatrixAdd = FALSE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCA_Candies,varname.colorup = "Candies",varname.colordown = "Candies", varname.pchup = "Judges", varname.pchdown = "Judges",)
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCA_Candies$Candies$var)

screeplotCandies <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCA_Candies$Judges$var)

screeplotJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCA_Candies$`Candies:Judges`$var)

screeplotCandiesJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCA_Candies$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Candies_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Candies,effectNames = c("Candies"),color = "Candies",shape = "Candies") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-20, 15), ylim = c(-10, 8))

Judges_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Candies,effectNames = c("Judges"),color = "Judges") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

# Inverser les scores et les loadings pour Candies:Judges
resLmpPcaEffectsASCA_Candies_bis <- resLmpPcaEffectsASCA_Candies
resLmpPcaEffectsASCA_Candies_bis$`Candies:Judges`$scores <- - resLmpPcaEffectsASCA_Candies$`Candies:Judges`$scores
resLmpPcaEffectsASCA_Candies_bis$`Candies:Judges`$loadings <- - resLmpPcaEffectsASCA_Candies$`Candies:Judges`$loadings

CA_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Candies_bis,effectNames = c("Candies:Judges"), color = "Judges",shape = "Candies", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

# 2 points outliers
index <- c(10, 27)
Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Candies,effectNames = c("Residuals"), shape = "Candies", color = "Judges") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 
  annotate("text", y = (resLmpPcaEffectsASCA_Candies$Residuals$scores[index,2] +
                                1.7*c(-1 , -1)),
                         x = resLmpPcaEffectsASCA_Candies$Residuals$scores[index,1],
                         label = rownames(resLmpPcaEffectsASCA_Candies$Residuals$scores[index,1:2]))

loadCandies <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Candies, effectNames = c("Candies"), addRownames = TRUE) 

loadJudges <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Candies, effectNames = c("Judges"), addRownames = TRUE) 

loadCandiesJudges <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Candies_bis, effectNames = c("Candies:Judges"), addRownames = TRUE) 

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Candies, effectNames = c("Residuals"), addRownames = TRUE) 

```

```{r}
a <- grid.arrange(screeplotCandies, Candies_scores,
              loadCandies, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Candy effect matrix",
                          gp=gpar(fontsize=20,font=2)))

a
```

```{r}
b <- grid.arrange(screeplotJudges, Judges_scores,
              loadJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Judge effect matrix",
                          gp=gpar(fontsize=20,font=2)))

b
```

```{r}
c <- grid.arrange(screeplotCandiesJudges, CA_scores,
              loadCandiesJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("C*J effect matrix",
                          gp=gpar(fontsize=20,font=2)))

c
```

```{r}
d <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))

d
```


```{r}
lmpEffectPlot(resLmpPcaEffectsASCA_Candies, effectName = "Candies:Judges", x = "Candies", z = "Judges")
```



#### Combiner Candies, Judges et Candies:Judges

```{r}
resLmpPcaEffectsASCA_Comb_Candies <- lmpPcaEffects(resLmpEffectMatrices_Candies, method="ASCA",verbose = TRUE, correctedMatrixAdd = FALSE, backtransform = TRUE,combineEffects = list(c("Candies","Judges", "Candies:Judges")))
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCA_Comb_Candies$`Candies+Judges+Candies:Judges`$var)

screeplotCombCandies <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()


combCandies_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Comb_Candies,effectNames = c("Candies+Judges+Candies:Judges"),color = "Candies",shape = "Candies", drawShapes = "polygon") + theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))


loadCombCandies <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Comb_Candies, effectNames = c("Candies+Judges+Candies:Judges"), addRownames = TRUE)



a <- grid.arrange(screeplotCombCandies, combCandies_scores,
              loadCombCandies, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Candies+Judges+Candies:Judges effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
# exporter
lmpEffectPlot(resLmpPcaEffectsASCA_Comb_Candies,effectName = c("Candies+Judges+Candies:Judges"), x = c("Candies"), z = c("Judges"),axes = c(1))

lmpEffectPlot(resLmpPcaEffectsASCA_Comb_Candies,effectName = c("Candies+Judges+Candies:Judges"), x = c("Candies"), z = c("Judges"),axes = c(2))
```

### Partie Serum

```{r}
resLmpPcaEffectsASCA_Serum <- lmpPcaEffects(resLmpEffectMatrices_Serum, method="ASCA",verbose = TRUE,backtransform = TRUE, correctedMatrixAdd = FALSE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCA_Serum,varname.colorup = "Volunteer",
                      varname.colordown = "Volunteer", varname.pchup = "Sampling", 
                      varname.pchdown = "Sampling")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCA_Serum$Volunteer$var)

screeplotVolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCA_Serum$`Volunteer:Sampling`$var)

screeplotVS <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCA_Serum$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Volunteer_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Serum,effectNames = c("Volunteer"),color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

VS_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Serum,effectNames = c("Volunteer:Sampling"), color = "Volunteer",shape = "Sampling", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Serum,effectNames = c("Residuals"), shape = "Sampling", color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadVolunteer <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Serum, effectNames = c("Volunteer")) 

loadVolunteerSampling <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Serum, effectNames = c("Volunteer:Sampling"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Serum, effectNames = c("Residuals"))

a <- grid.arrange(screeplotVolunteer, Volunteer_scores,
              loadVolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))

a
```

```{r}
b <- grid.arrange(screeplotVS, VS_scores,
              loadVolunteerSampling, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer:Sampling effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```


```{r}
c <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```


```{r}
lmpEffectPlot(resLmpPcaEffectsASCA_Serum, effectName = "Volunteer:Sampling", x = "Sampling", z = "Volunteer")
```

### Partie CHOO

```{r}
resLmpPcaEffectsASCA_CHOO <- lmpPcaEffects(resLmpEffectMatrices_CHOO, method="ASCA",verbose = TRUE, correctedMatrixAdd = FALSE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCA_CHOO,varname.colorup = "time",varname.colordown = "time", varname.pchup = "treatment", varname.pchdown = "treatment")
```



```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_CHOO$volunteer$var)

screeplotvolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_CHOO$treatment$var)

screeplottreatment <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_CHOO$time$var)

screeplottime <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_CHOO$`treatment:time`$var)

screeplottrti <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_CHOO$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

volunteer_scores <- lmpScorePlot(resLmpPcaEffectsASCA_CHOO,effectNames = c("volunteer"),color = "volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

treatment_scores <- lmpScorePlot(resLmpPcaEffectsASCA_CHOO,effectNames = c("treatment"), color = "treatment",shape = "treatment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

time_scores <- lmpScorePlot(resLmpPcaEffectsASCA_CHOO,effectNames = c("time"), color = "time",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

treatmenttime_scores <- lmpScorePlot(resLmpPcaEffectsASCA_CHOO,effectNames = c("treatment:time"), color = "treatment",shape = "time", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCA_CHOO,effectNames = c("Residuals"), shape = "treatment", color = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadvolunteer <- lmpLoading2dPlot(resLmpPcaEffectsASCA_CHOO, effectNames = c("volunteer")) 

loadtreatment <- lmpLoading2dPlot(resLmpPcaEffectsASCA_CHOO, effectNames = c("treatment"))

loadtime <- lmpLoading2dPlot(resLmpPcaEffectsASCA_CHOO, effectNames = c("time"))

loadtreatmenttime <- lmpLoading2dPlot(resLmpPcaEffectsASCA_CHOO, effectNames = c("treatment:time"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCA_CHOO, effectNames = c("Residuals"))


```

```{r}
a <- grid.arrange(screeplotvolunteer, volunteer_scores,
              loadvolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplottreatment, treatment_scores,
              loadtreatment, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```
```{r}
c <- grid.arrange(screeplottime, time_scores,
              loadtime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplottrti, treatmenttime_scores,
              loadtreatmenttime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment:time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

```{r}
e <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
e
```


```{r}
lmpEffectPlot(resLmpPcaEffectsASCA_CHOO, effectName = "treatment:time", x = "time", z = "treatment")
```

```{r}
lmpEffectPlot(resLmpPcaEffectsASCA_CHOO, effectName = "treatment:time", x = "treatment", z = "time")
```

#### ASCA avec combinaisons 

Il y a 2 combinaisons d'effets :
 - treatment et time 
 - treatment, time et volunteer

```{r}
resLmpPcaEffectsASCA_Comb_CHOO <- lmpPcaEffects(resLmpEffectMatrices_CHOO, method="ASCA",verbose = TRUE, combineEffects = list(c("treatment","time"), c("treatment","time","volunteer")),correctedMatrixAdd = FALSE)
```
```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_Comb_CHOO$`treatment+time`$var)

screeplotComb <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCA_Comb_CHOO$`treatment+time+volunteer`$var)

screeplotComb2 <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

comb_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Comb_CHOO,effectNames = c("treatment+time"), color = "treatment",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

comb2_scores <- lmpScorePlot(resLmpPcaEffectsASCA_Comb_CHOO,effectNames = c("treatment+time+volunteer"), color = "treatment",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

loadComb <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Comb_CHOO, effectNames = c("treatment+time")) 

loadComb2 <- lmpLoading2dPlot(resLmpPcaEffectsASCA_Comb_CHOO, effectNames = c("treatment+time+volunteer"))
```

```{r}
a <- grid.arrange(screeplotComb, comb_scores,
              loadComb, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment+time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotComb2, comb2_scores,
              loadComb2, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment+time+volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```

### Partie UCH

```{r}
resLmpPcaEffectsASCA_UCH <- lmpPcaEffects(resLmpEffectMatrices_UCH, method="ASCA",verbose = TRUE, correctedMatrixAdd = FALSE)
```

```{r}
all_loadings_pl <- lmpLoading1dPlot(resLmpPcaEffectsASCA_UCH,
                              effectNames = c("Hippurate", "Citrate","Time",
                                              "Hippurate:Time",
                                              "Residuals"),
                              axes = 1, xlab = "ppm")
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCA_UCH,PCdim=c(1,1,1,1,1,1,1,2),
                     modelAbbrev = TRUE,
                     varname.colorup = "Citrate",
                     varname.colordown  = "Time",
                     varname.pchup="Hippurate",
                     varname.pchdown="Time",
                     title = "ASCA scores scatterplot matrix")
```

```{r}
# Hippurate
hip_scores_pl <- lmpScorePlot(resLmpPcaEffectsASCA_UCH, effectNames = "Hippurate", 
             color = "Hippurate", shape = "Hippurate")

hip_loadings_pl <- all_loadings_pl$Hippurate

grid.arrange(hip_scores_pl,hip_loadings_pl, ncol=2)
```
```{r}
# Citrate
cit_scores_pl <- lmpScorePlot(resLmpPcaEffectsASCA_UCH, effectNames = "Citrate", 
             color = "Citrate", shape = "Citrate")
cit_loadings_pl <- all_loadings_pl$Citrate

grid.arrange(cit_scores_pl,cit_loadings_pl, ncol=2)
```
```{r}
# Time
tim_scores_pl <- lmpScorePlot(resLmpPcaEffectsASCA_UCH, effectNames = "Time", color = "Time", 
                              shape = "Time")

tim_loadings_pl <- all_loadings_pl$Time

grid.arrange(tim_scores_pl,tim_loadings_pl, ncol=2)
```
```{r}
# Hippurate:Time
hiptim_scores_pl <- lmpScorePlot(resLmpPcaEffectsASCA_UCH, effectNames = "Hippurate:Time", 
                                 color = "Hippurate", shape = "Time")
hiptim_loadings_pl <- all_loadings_pl$`Hippurate:Time` 

grid.arrange(hiptim_scores_pl,hiptim_loadings_pl, ncol=2)
```
```{r}
resid_scores_pl <- lmpScorePlot(resLmpPcaEffectsASCA_UCH, effectNames = "Residuals",
                                color = "Day", shape = "Day", 
                                drawShapes = "segment")


resid_loadings_pl <- all_loadings_pl$Residuals 

grid.arrange(resid_scores_pl,resid_loadings_pl, ncol=2)
```

```{r}
UPDATE_lmpEffectPlot(resLmpPcaEffectsASCA_UCH, effectName = "Hippurate:Time", x = "Hippurate", z = "Time")
```

#### ASCA avec combinaisons d'effets 

Il y a 2 combinaisons d'effets :
 - Hippurate, Time et Hippurate:Time 
 - Hippurate et Times

```{r}
resLmpPcaEffectsASCA_Comb_UCH <- lmpPcaEffects(resLmpEffectMatrices_UCH, method="ASCA",verbose = TRUE, combineEffects = list(c("Hippurate", "Time", "Hippurate:Time"),c("Hippurate", "Time")))
```
```{r}
comb_loadings_pl <- lmpLoading1dPlot(resLmpPcaEffectsASCA_Comb_UCH,
                              effectNames = c("Hippurate+Time+Hippurate:Time"),
                              axes = 1, xlab = "ppm")
```

```{r}
# Hippurate+Time+Hippurate:Time
hiptimInter_scores_pl <- lmpScorePlot(resLmpPcaEffectsASCA_Comb_UCH, 
                            effectNames = "Hippurate+Time+Hippurate:Time", 
                            color = "Hippurate", shape = "Time")

hiptimInter_loadings_pl <- all_loadings_pl$`Hippurate:Time`

grid.arrange(hiptimInter_scores_pl,hiptimInter_loadings_pl, ncol=2)
```

## APCA

### Partie Candies

#### APCA sans correction de la matrice augmentée

```{r}
resLmpPcaEffectsAPCA_Candies <- lmpPcaEffects(resLmpEffectMatrices_Candies, 
                                    method="APCA",
                                    verbose = TRUE,
                                    backtransform = TRUE, 
                                    correctedMatrixAdd = FALSE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_Candies,
                          varname.colorup = "Candies",
                          varname.colordown = "Candies", 
                          varname.pchup = "Judges", 
                          varname.pchdown = "Judges")
```


```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies$Candies$var)

screeplotCandies <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies$Judges$var)

screeplotJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies$`Candies:Judges`$var)

screeplotCandiesJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Candies_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies,effectNames = c("Candies"),color = "Candies",shape = "Candies", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

Judges_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies,effectNames = c("Judges"),color = "Judges", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

# Inverser les scores et les loadings pour Candies:Judges
resLmpPcaEffectsAPCA_Candies_bis <- resLmpPcaEffectsAPCA_Candies
resLmpPcaEffectsAPCA_Candies_bis$`Candies:Judges`$scores <- - resLmpPcaEffectsAPCA_Candies$`Candies:Judges`$scores
resLmpPcaEffectsAPCA_Candies_bis$`Candies:Judges`$loadings <- - resLmpPcaEffectsAPCA_Candies$`Candies:Judges`$loadings

CA_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies_bis,effectNames = c("Candies:Judges"), color = "Judges",shape = "Candies", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

# 2 points outliers
index <- c(10, 27)
Residuals_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies,effectNames = c("Residuals"), shape = "Candies", color = "Judges") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-20, 20), ylim = c(-20, 20)) +
  annotate("text", y = (resLmpPcaEffectsAPCA_Candies$Residuals$scores[index,2] +
                                1.7*c(-1 , -1)),
                         x = resLmpPcaEffectsAPCA_Candies$Residuals$scores[index,1],
                         label = rownames(resLmpPcaEffectsAPCA_Candies$Residuals$scores[index,1:2]))

loadCandies <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies, effectNames = c("Candies"), addRownames = TRUE) 

loadJudges <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies, effectNames = c("Judges"), addRownames = TRUE) 

loadCandiesJudges <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies_bis, effectNames = c("Candies:Judges"), addRownames = TRUE) 

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies, effectNames = c("Residuals"), addRownames = TRUE) 
```

```{r}
a <- grid.arrange(screeplotCandies, Candies_scores,
              loadCandies, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Candy effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotJudges, Judges_scores,
              loadJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Judge effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```

```{r}
c <- grid.arrange(screeplotCandiesJudges, CA_scores,
              loadCandiesJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("C*J effect matrix",
                          gp=gpar(fontsize=20,font=2)))
ggsave(file = file.path(output_Candies,"Candies_APCA_CJ.jpeg"),c, width = 30, height = 13, units = "cm",dpi = 1000, scale = 0.8)
```

```{r}
d <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

#### APCA avec correction de la matrice augmentée

```{r}
resLmpPcaEffectsAPCA_Candies_corr <- lmpPcaEffects(resLmpEffectMatrices_Candies, method="APCA",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = TRUE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_Candies_corr,varname.colorup = "Candies",varname.colordown = "Candies", varname.pchup = "Judges", varname.pchdown = "Judges",)
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies_corr$Candies$var)

screeplotCandies <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies_corr$Judges$var)

screeplotJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies_corr$`Candies:Judges`$var)

screeplotCandiesJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsAPCA_Candies_corr$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Candies_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies_corr,effectNames = c("Candies"),color = "Candies",shape = "Candies", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Judges_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies_corr,effectNames = c("Judges"),color = "Judges", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

# Inverser les scores et les loadings pour Candies:Judges
resLmpPcaEffectsAPCA_Candies_corr_bis <- resLmpPcaEffectsAPCA_Candies_corr
resLmpPcaEffectsAPCA_Candies_corr_bis$`Candies:Judges`$scores <- - resLmpPcaEffectsAPCA_Candies_corr$`Candies:Judges`$scores
resLmpPcaEffectsAPCA_Candies_corr_bis$`Candies:Judges`$loadings <- - resLmpPcaEffectsAPCA_Candies_corr$`Candies:Judges`$loadings

CA_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies_corr_bis,effectNames = c("Candies:Judges"), color = "Judges",shape = "Candies", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

# 2 points outliers
index <- c(10, 27)
Residuals_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Candies_corr,effectNames = c("Residuals"), shape = "Candies", color = "Judges") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-20, 20), ylim = c(-20, 20)) +
  annotate("text", y = (resLmpPcaEffectsAPCA_Candies_corr$Residuals$scores[index,2] +
                                1.7*c(-1 , -1)),
                         x = resLmpPcaEffectsAPCA_Candies_corr$Residuals$scores[index,1],
                         label = rownames(resLmpPcaEffectsAPCA_Candies_corr$Residuals$scores[index,1:2]))

loadCandies <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies_corr, effectNames = c("Candies"), addRownames = TRUE)

loadJudges <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies_corr, effectNames = c("Judges"), addRownames = TRUE)

loadCandiesJudges <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies_corr_bis, effectNames = c("Candies:Judges"), addRownames = TRUE)

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Candies_corr, effectNames = c("Residuals"), addRownames = TRUE) 
```

```{r}
a <- grid.arrange(screeplotCandies, Candies_scores,
              loadCandies, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Candy effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotJudges, Judges_scores,
              loadJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Judge effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```

```{r}
c <- grid.arrange(screeplotCandiesJudges, CA_scores,
              loadCandiesJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("C*J effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

### Partie Serum

#### APCA sans correction de la matrice augmentée

```{r}
resLmpPcaEffectsAPCA_Serum <- lmpPcaEffects(resLmpEffectMatrices_Serum, method="APCA",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = FALSE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_Serum,varname.colorup = "Volunteer",varname.colordown = "Volunteer", varname.pchup = "Sampling", varname.pchdown = "Sampling")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsAPCA_Serum$Volunteer$var)

screeplotVolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsAPCA_Serum$`Volunteer:Sampling`$var)

screeplotVS <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsAPCA_Serum$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Volunteer_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Serum,effectNames = c("Volunteer"),color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

VS_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Serum,effectNames = c("Volunteer:Sampling"), color = "Volunteer",shape = "Sampling", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Serum,effectNames = c("Residuals"), shape = "Sampling", color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadVolunteer <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Serum, effectNames = c("Volunteer")) 

loadVolunteerSampling <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Serum, effectNames = c("Volunteer:Sampling"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Serum, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotVolunteer, Volunteer_scores,
              loadVolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotVS, VS_scores,
              loadVolunteerSampling, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer:Sampling effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```


```{r}
c <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

#### APCA avec correction de la matrice augmentée

```{r}
resLmpPcaEffectsAPCA_Serum_corr <- lmpPcaEffects(resLmpEffectMatrices_Serum, method="APCA",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = TRUE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_Serum_corr,varname.colorup = "Volunteer",varname.colordown = "Volunteer", varname.pchup = "Sampling", varname.pchdown = "Sampling")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsAPCA_Serum_corr$Volunteer$var)

screeplotVolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsAPCA_Serum_corr$`Volunteer:Sampling`$var)

screeplotVS <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsAPCA_Serum_corr$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Volunteer_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Serum_corr,effectNames = c("Volunteer"),color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

VS_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Serum_corr,effectNames = c("Volunteer:Sampling"), color = "Volunteer",shape = "Sampling", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_Serum_corr,effectNames = c("Residuals"), shape = "Sampling", color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadVolunteer <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Serum_corr, effectNames = c("Volunteer")) 

loadVolunteerSampling <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Serum_corr, effectNames = c("Volunteer:Sampling"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_Serum_corr, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotVolunteer, Volunteer_scores,
              loadVolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```
```{r}
b <- grid.arrange(screeplotVS, VS_scores,
              loadVolunteerSampling, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer:Sampling effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```


```{r}
c <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

### Partie CHOO

#### APCA sans correction de la matrice augmentée

```{r}
resLmpPcaEffectsAPCA_CHOO <- lmpPcaEffects(resLmpEffectMatrices_CHOO, method="APCA",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = FALSE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_CHOO,varname.colorup = "time",varname.colordown = "time", varname.pchup = "treatment", varname.pchdown = "treatment")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO$volunteer$var)

screeplotvolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO$treatment$var)

screeplottreatment <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO$time$var)

screeplottime <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO$`treatment:time`$var)

screeplottrti <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

volunteer_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO,effectNames = c("volunteer"),color = "volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

treatment_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO,effectNames = c("treatment"), color = "treatment",shape = "treatment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

time_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO,effectNames = c("time"), color = "time",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

treatmenttime_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO,effectNames = c("treatment:time"), color = "treatment",shape = "time", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO,effectNames = c("Residuals"), shape = "treatment", color = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadvolunteer <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO, effectNames = c("volunteer")) 

loadtreatment <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO, effectNames = c("treatment"))

loadtime <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO, effectNames = c("time"))

loadtreatmenttime <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO, effectNames = c("treatment:time"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotvolunteer, volunteer_scores,
              loadvolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```
```{r}
b <- grid.arrange(screeplottreatment, treatment_scores,
              loadtreatment, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```
```{r}
c <- grid.arrange(screeplottime, time_scores,
              loadtime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplottrti, treatmenttime_scores,
              loadtreatmenttime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment:time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

```{r}
e <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
e
```

#### APCA avec correction de la matrice augmentée

```{r}
resLmpPcaEffectsAPCA_CHOO_corr <- lmpPcaEffects(resLmpEffectMatrices_CHOO, method="APCA",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = TRUE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_CHOO_corr,varname.colorup = "time",varname.colordown = "time", varname.pchup = "treatment", varname.pchdown = "treatment")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO_corr$volunteer$var)

screeplotvolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO_corr$treatment$var)

screeplottreatment <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO_corr$time$var)

screeplottime <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO_corr$`treatment:time`$var)

screeplottrti <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsAPCA_CHOO_corr$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

volunteer_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO_corr,effectNames = c("volunteer"),color = "volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

treatment_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO_corr,effectNames = c("treatment"), color = "treatment",shape = "treatment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

time_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO_corr,effectNames = c("time"), color = "time",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

treatmenttime_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO_corr,effectNames = c("treatment:time"), color = "treatment",shape = "time", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsAPCA_CHOO_corr,effectNames = c("Residuals"), shape = "treatment", color = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadvolunteer <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO_corr, effectNames = c("volunteer")) 

loadtreatment <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO_corr, effectNames = c("treatment"))

loadtime <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO_corr, effectNames = c("time"))

loadtreatmenttime <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO_corr, effectNames = c("treatment:time"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsAPCA_CHOO_corr, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotvolunteer, volunteer_scores,
              loadvolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```
```{r}
b <- grid.arrange(screeplottreatment, treatment_scores,
              loadtreatment, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```
```{r}
c <- grid.arrange(screeplottime, time_scores,
              loadtime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplottrti, treatmenttime_scores,
              loadtreatmenttime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment:time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

```{r}
e <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
e
```

### Partie UCH

```{r}
resLmpPcaEffectsAPCA_UCH <- lmpPcaEffects(resLmpEffectMatrices_UCH, method="APCA",verbose = TRUE, correctedMatrixAdd = FALSE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsAPCA_UCH,
                     effectNames = c("Hippurate", "Citrate", "Time",
                                     "Hippurate:Time"),
                     modelAbbrev = TRUE,
                     varname.colorup = "Citrate",
                     varname.colordown  = "Time",
                     varname.pchup="Hippurate",
                     varname.pchdown="Time",
                     title = "APCA scores scatterplot matrix")
```


```{r}
# Hippurate main effect
lmpScorePlot(resLmpPcaEffectsAPCA_UCH, effectNames = "Hippurate", 
             color = "Hippurate", shape = "Hippurate", drawShapes = "ellipse")
```
```{r}
# Citrate main effect
lmpScorePlot(resLmpPcaEffectsAPCA_UCH, effectNames = "Citrate", 
             color = "Citrate", shape = "Citrate", drawShapes = "ellipse")
```
```{r}
# Time main effect
lmpScorePlot(resLmpPcaEffectsAPCA_UCH, effectNames = "Time", 
             color = "Time", shape = "Time", drawShapes = "ellipse")
```
```{r}
# Interaction term
lmpScorePlot(resLmpPcaEffectsAPCA_UCH, effectNames = "Hippurate:Time", 
             color = "Hippurate", shape = "Time", drawShapes = "segment")
```
```{r}
lmpLoading1dPlot(resLmpPcaEffectsAPCA_UCH, effectNames = c("Hippurate", "Citrate", 
                                        "Time", "Hippurate:Time"), axes = 1)
```

## ASCA-E

### Partie Candies

#### ASCA-E sans correction de la matrice augmentée

```{r}
resLmpPcaEffectsASCAE_Candies <- lmpPcaEffects(resLmpEffectMatrices_Candies, method="ASCA-E",verbose = TRUE, backtransform = TRUE,  correctedMatrixAdd = FALSE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_Candies,varname.colorup = "Candies",varname.colordown = "Candies", varname.pchup = "Judges", varname.pchdown = "Judges",)
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies$Candies$var)

screeplotCandies <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies$Judges$var)

screeplotJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies$`Candies:Judges`$var)

screeplotCandiesJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Candies_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies,effectNames = c("Candies"),color = "Candies",shape = "Candies", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Judges_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies,effectNames = c("Judges"),color = "Judges", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

# Inverser les scores et les loadings pour Candies:Judges
resLmpPcaEffectsASCAE_Candies_bis <- resLmpPcaEffectsASCAE_Candies
resLmpPcaEffectsASCAE_Candies_bis$`Candies:Judges`$scores <- - resLmpPcaEffectsASCAE_Candies$`Candies:Judges`$scores
resLmpPcaEffectsASCAE_Candies_bis$`Candies:Judges`$loadings <- - resLmpPcaEffectsASCAE_Candies$`Candies:Judges`$loadings

CA_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies_bis,effectNames = c("Candies:Judges"), color = "Judges",shape = "Candies", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

# 2 points outliers
index <- c(10, 27)
Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies,effectNames = c("Residuals"), shape = "Candies", color = "Judges") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-20, 20), ylim = c(-20, 20)) +
  annotate("text", y = (resLmpPcaEffectsASCAE_Candies$Residuals$scores[index,2] +
                                1.7*c(-1 , -1)),
                         x = resLmpPcaEffectsASCAE_Candies$Residuals$scores[index,1],
                         label = rownames(resLmpPcaEffectsASCAE_Candies$Residuals$scores[index,1:2]))

loadCandies <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies, effectNames = c("Candies"), addRownames = TRUE)

loadJudges <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies, effectNames = c("Judges"), addRownames = TRUE)

loadCandiesJudges <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies_bis, effectNames = c("Candies:Judges"), addRownames = TRUE)

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies, effectNames = c("Residuals"), addRownames = TRUE) 

```

```{r}
a <- grid.arrange(screeplotCandies, Candies_scores,
              loadCandies, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Candy effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotJudges, Judges_scores,
              loadJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Judge effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```

```{r}
c <- grid.arrange(screeplotCandiesJudges, CA_scores,
              loadCandiesJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("C*J effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

#### ASCA-E avec correction de la matrice augmentée

```{r}
resLmpPcaEffectsASCAE_Candies_corr <- lmpPcaEffects(resLmpEffectMatrices_Candies, method="ASCA-E",verbose = TRUE, backtransform = TRUE,  correctedMatrixAdd = TRUE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_Candies_corr,varname.colorup = "Candies",varname.colordown = "Candies", varname.pchup = "Judges", varname.pchdown = "Judges",)
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies_corr$Candies$var)

screeplotCandies <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies_corr$Judges$var)

screeplotJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies_corr$`Candies:Judges`$var)

screeplotCandiesJudges <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:8), var = resLmpPcaEffectsASCAE_Candies_corr$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Candies_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies_corr,effectNames = c("Candies"),color = "Candies",shape = "Candies", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-20, 15), ylim = c(-10, 8))

Judges_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies_corr,effectNames = c("Judges"),color = "Judges", drawShapes = "ellipse") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-7, 9), ylim = c(-5, 3))

# Inverser les scores et les loadings pour Candies:Judges
resLmpPcaEffectsASCAE_Candies_corr_bis <- resLmpPcaEffectsASCAE_Candies_corr
resLmpPcaEffectsASCAE_Candies_corr_bis$`Candies:Judges`$scores <- - resLmpPcaEffectsASCAE_Candies_corr$`Candies:Judges`$scores
resLmpPcaEffectsASCAE_Candies_corr_bis$`Candies:Judges`$loadings <- - resLmpPcaEffectsASCAE_Candies_corr$`Candies:Judges`$loadings

CA_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies_corr_bis,effectNames = c("Candies:Judges"), color = "Judges",shape = "Candies", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-11, 9), ylim = c(-8, 11))

# 2 points outliers
index <- c(10, 27)
Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Candies_corr,effectNames = c("Residuals"), shape = "Candies", color = "Judges") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) +
  coord_cartesian(xlim = c(-20, 20), ylim = c(-20, 20)) +
  annotate("text", y = (resLmpPcaEffectsASCAE_Candies_corr$Residuals$scores[index,2] +
                                1.7*c(-1 , -1)),
                         x = resLmpPcaEffectsASCAE_Candies_corr$Residuals$scores[index,1],
                         label = rownames(resLmpPcaEffectsASCAE_Candies_corr$Residuals$scores[index,1:2]))

loadCandies <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies_corr, effectNames = c("Candies"), addRownames = TRUE) +
  coord_cartesian(xlim = c(-0.5, 0.5), ylim = c(-1, 1))

loadJudges <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies_corr, effectNames = c("Judges"), addRownames = TRUE) +
  coord_cartesian(xlim = c(-0.8, 0.8), ylim = c(-0.6, 0.6))

loadCandiesJudges <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies_corr_bis, effectNames = c("Candies:Judges"), addRownames = TRUE) +
  coord_cartesian(xlim = c(-0.8, 0.8), ylim = c(-0.6, 0.6))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Candies_corr, effectNames = c("Residuals"), addRownames = TRUE) +
  coord_cartesian(xlim = c(-0.8, 0.8), ylim = c(-0.5, 0.5))
```

```{r}
a <- grid.arrange(screeplotCandies, Candies_scores,
              loadCandies, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Candy effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```
```{r}
b <- grid.arrange(screeplotJudges, Judges_scores,
              loadJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Judge effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```


```{r}
c <- grid.arrange(screeplotCandiesJudges, CA_scores,
              loadCandiesJudges, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("C*J effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

### Partie Serum

#### ASCA-E sans correction de la matrice augmentée

```{r}
resLmpPcaEffectsASCAE_Serum <- lmpPcaEffects(resLmpEffectMatrices_Serum, method="ASCA-E",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = FALSE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_Serum,varname.colorup = "Volunteer",varname.colordown = "Volunteer", varname.pchup = "Sampling", varname.pchdown = "Sampling")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCAE_Serum$Volunteer$var)

screeplotVolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCAE_Serum$`Volunteer:Sampling`$var)

screeplotVS <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCAE_Serum$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Volunteer_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Serum,effectNames = c("Volunteer"),color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

VS_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Serum,effectNames = c("Volunteer:Sampling"), color = "Volunteer",shape = "Sampling", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Serum,effectNames = c("Residuals"), shape = "Sampling", color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadVolunteer <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Serum, effectNames = c("Volunteer")) 

loadVolunteerSampling <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Serum, effectNames = c("Volunteer:Sampling"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Serum, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotVolunteer, Volunteer_scores,
              loadVolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotVS, VS_scores,
              loadVolunteerSampling, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer:Sampling effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```

```{r}
c <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

#### ASCA-E avec correction de la matrice augmentée

```{r}
resLmpPcaEffectsASCAE_Serum_corr <- lmpPcaEffects(resLmpEffectMatrices_Serum, method="ASCA-E",verbose = TRUE, backtransform = TRUE ,correctedMatrixAdd = TRUE)
```

```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_Serum_corr,varname.colorup = "Volunteer",varname.colordown = "Volunteer", varname.pchup = "Sampling", varname.pchdown = "Sampling")
```


```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCAE_Serum_corr$Volunteer$var)

screeplotVolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCAE_Serum_corr$`Volunteer:Sampling`$var)

screeplotVS <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:15), var = resLmpPcaEffectsASCAE_Serum_corr$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

Volunteer_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Serum_corr,effectNames = c("Volunteer"),color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

VS_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Serum_corr,effectNames = c("Volunteer:Sampling"), color = "Volunteer",shape = "Sampling", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_Serum_corr,effectNames = c("Residuals"), shape = "Sampling", color = "Volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadVolunteer <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Serum_corr, effectNames = c("Volunteer")) 

loadVolunteerSampling <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Serum_corr, effectNames = c("Volunteer:Sampling"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_Serum_corr, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotVolunteer, Volunteer_scores,
              loadVolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplotVS, VS_scores,
              loadVolunteerSampling, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Volunteer:Sampling effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```

```{r}
c <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

### Partie CHOO

#### ASCA-E sans correction de la matrice augmentée

```{r}
resLmpPcaEffectsASCAE_CHOO <- lmpPcaEffects(resLmpEffectMatrices_CHOO, method="ASCA-E",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = FALSE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_CHOO,varname.colorup = "time",varname.colordown = "time", varname.pchup = "treatment", varname.pchdown = "treatment")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO$volunteer$var)

screeplotvolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO$treatment$var)

screeplottreatment <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO$time$var)

screeplottime <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO$`treatment:time`$var)

screeplottrti <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

volunteer_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO,effectNames = c("volunteer"),color = "volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

treatment_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO,effectNames = c("treatment"), color = "treatment",shape = "treatment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

time_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO,effectNames = c("time"), color = "time",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

treatmenttime_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO,effectNames = c("treatment:time"), color = "treatment",shape = "time", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO,effectNames = c("Residuals"), shape = "treatment", color = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadvolunteer <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO, effectNames = c("volunteer")) 

loadtreatment <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO, effectNames = c("treatment"))

loadtime <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO, effectNames = c("time"))

loadtreatmenttime <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO, effectNames = c("treatment:time"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotvolunteer, volunteer_scores,
              loadvolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplottreatment, treatment_scores,
              loadtreatment, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```
```{r}
c <- grid.arrange(screeplottime, time_scores,
              loadtime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplottrti, treatmenttime_scores,
              loadtreatmenttime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment:time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

```{r}
e <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
e
```

#### ASCA-E avec correction de la matrice augmentée

```{r}
resLmpPcaEffectsASCAE_CHOO_corr <- lmpPcaEffects(resLmpEffectMatrices_CHOO, method="ASCA-E",verbose = TRUE, backtransform = TRUE, correctedMatrixAdd = TRUE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_CHOO_corr,varname.colorup = "time",varname.colordown = "time", varname.pchup = "treatment", varname.pchdown = "treatment")
```

```{r}
# Construction des graphiques
df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO_corr$volunteer$var)

screeplotvolunteer <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO_corr$treatment$var)

screeplottreatment <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO_corr$time$var)

screeplottime <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO_corr$`treatment:time`$var)

screeplottrti <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

df <- data.frame(PC = as.character(1:14), var = resLmpPcaEffectsASCAE_CHOO_corr$Residuals$var)

screeplotResiduals <- ggplot(df, aes(y=0,yend=var,x = PC,
                          xend=PC))+ geom_segment() +
                   labs(title= "Scree plot",
                        x = "PC", y="% var") + theme_classic()

volunteer_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO_corr,effectNames = c("volunteer"),color = "volunteer") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

treatment_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO_corr,effectNames = c("treatment"), color = "treatment",shape = "treatment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

time_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO_corr,effectNames = c("time"), color = "time",shape = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

treatmenttime_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO_corr,effectNames = c("treatment:time"), color = "treatment",shape = "time", drawShapes = "segment") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line"))

Residuals_scores <- lmpScorePlot(resLmpPcaEffectsASCAE_CHOO_corr,effectNames = c("Residuals"), shape = "treatment", color = "time") + 
  theme(legend.text=element_text(size=10),
        legend.key.height=unit(0.7,"line")) 

loadvolunteer <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO_corr, effectNames = c("volunteer")) 

loadtreatment <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO_corr, effectNames = c("treatment"))

loadtime <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO_corr, effectNames = c("time"))

loadtreatmenttime <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO_corr, effectNames = c("treatment:time"))

loadResiduals <- lmpLoading2dPlot(resLmpPcaEffectsASCAE_CHOO_corr, effectNames = c("Residuals"))
```

```{r}
a <- grid.arrange(screeplotvolunteer, volunteer_scores,
              loadvolunteer, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("volunteer effect matrix",
                          gp=gpar(fontsize=20,font=2)))
a
```

```{r}
b <- grid.arrange(screeplottreatment, treatment_scores,
              loadtreatment, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment effect matrix",
                          gp=gpar(fontsize=20,font=2)))
b
```
```{r}
c <- grid.arrange(screeplottime, time_scores,
              loadtime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
c
```

```{r}
d <- grid.arrange(screeplottrti, treatmenttime_scores,
              loadtreatmenttime, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("treatment:time effect matrix",
                          gp=gpar(fontsize=20,font=2)))
d
```

```{r}
e <- grid.arrange(screeplotResiduals, Residuals_scores,
              loadResiduals, nrow=1,widths=c(0.3, 1,  0.85),
             top=textGrob("Residuals effect matrix",
                          gp=gpar(fontsize=20,font=2)))
e
```

### Partie UCH

```{r}
resLmpPcaEffectsASCAE_UCH <- lmpPcaEffects(resLmpEffectMatrices_UCH, method="ASCA-E",verbose = TRUE, correctedMatrixAdd = FALSE)
```
```{r}
lmpScoreScatterPlotM(resLmpPcaEffectsASCAE_UCH,
                     effectNames = c("Hippurate", "Citrate", "Time",
                                     "Hippurate:Time"),
                     modelAbbrev = TRUE,
                     varname.colorup = "Citrate",
                     varname.colordown  = "Time",
                     varname.pchup="Hippurate",
                     varname.pchdown="Time",
                     title = "ASCA-E scores scatterplot matrix")
```


```{r}
lmpScorePlot(resLmpPcaEffectsASCAE_UCH, effectNames = "Hippurate", 
             color = "Hippurate", shape = "Hippurate")
```
```{r}
lmpScorePlot(resLmpPcaEffectsASCAE_UCH, effectNames = "Citrate", 
             color = "Citrate", shape = "Citrate")
```
```{r}
lmpScorePlot(resLmpPcaEffectsASCAE_UCH, effectNames = "Time", 
             color = "Time", shape = "Time")
```
```{r}
lmpScorePlot(resLmpPcaEffectsASCAE_UCH, effectNames = "Hippurate:Time", 
             color = "Hippurate", shape = "Time")
```


# Contributions des effets ASCA

### Candies

```{r}
resContribution_Candies <- lmpContributions(resLmpPcaEffectsASCA_Candies)
```

#### Pourcentage de variance expliqué des réponses pour chaque effet 

```{r}
pander::pander(resContribution_Candies$effectTable)
```

```{r}
lmpScreePlot(resContribution_Candies)
```

#### Contributions des réponses pour chaque effet

```{r}
pander::pander(resContribution_Candies$contribTable)
```

#### Les plus grandes contributions des réponses pour chaque effet

```{r}
resContribution_Candies$plotContrib
# exporter le graphique
ggsave(file = file.path(output_Candies,"Candies_plotContrib.jpeg"),resContribution_Candies$plotContrib)
```

### Serum

```{r}
resContribution_Serum <- lmpContributions(resLmpPcaEffectsASCA_Serum)
```

#### Pourcentage de variance expliquée des réponses pour chaque effet 

```{r}
pander::pander(resContribution_Serum$effectTable)
```

```{r}
lmpScreePlot(resContribution_Serum)
```

#### Contributions des réponses pour chaque effet

```{r}
pander::pander(resContribution_Serum$contribTable)
```

#### Les plus grandes contributions des réponses pour chaque effet

```{r}
resContribution_Serum$plotContrib
# exporter le graphique
ggsave(file = file.path(output_Serum,"Serum_plotContrib.jpeg"),resContribution_Serum$plotContrib)
```

### CHOO

```{r}
resContribution_CHOO <- lmpContributions(resLmpPcaEffectsASCA_CHOO)
```

#### Pourcentage de variance expliquée des réponses pour chaque effet 

```{r}
pander::pander(resContribution_CHOO$effectTable)
```

```{r}
lmpScreePlot(resContribution_CHOO)
```

#### Contributions des réponses pour chaque effet

```{r}
pander::pander(resContribution_CHOO$contribTable)
```

#### Les plus grandes contributions des réponses pour chaque effet

```{r}
resContribution_CHOO$plotContrib
# exporter le graphique
ggsave(file = file.path(output_CHOO,"CHOO_plotContrib.jpeg"),resContribution_CHOO$plotContrib)
```

### UCH

```{r}
resContribution_UCH <- lmpContributions(resLmpPcaEffectsASCA_UCH)
```

#### Pourcentage de variance expliquée des réponses pour chaque effet 

```{r}
pander::pander(resContribution_UCH$effectTable)
```

```{r}
lmpScreePlot(resContribution_UCH)
```

#### Contributions des réponses pour chaque effet

```{r}
pander::pander(resContribution_UCH$contribTable)
```

#### Les plus grandes contributions des réponses pour chaque effet

```{r}
resContribution_UCH$plotContrib
# exporter le graphique
ggsave(file = file.path(output_UCH,"UCH_plotContrib.jpeg"),resContribution_UCH$plotContrib)
```

# Info de la Session

```{r}
sessionInfo()
```

